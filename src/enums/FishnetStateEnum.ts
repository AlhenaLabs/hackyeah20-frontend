export enum FishnetStateEnum {
    LOST = 'lost',
    NEW = 'new',
    PURCHASED = 'purchased',
    NEEDS_RENEWAL = 'needs renewal',
    IN_USE = 'in use',
    DAMAGED = 'damaged',
    RETURNED = 'returned',
    RENEWED = 'renewed'
}